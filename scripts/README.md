# Repository-wide Scripts

Utility scripts shared across experiments (e.g. environment/hardware metadata capture,
results directory scaffolding). Experiment-specific scripts live inside each
`experiments/NN-name/scripts/` directory instead.
